<?xml version="1.0" encoding="utf-8" ?>
<Shell
    x:Class="PadariaFrontEndApp.AppShell" xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PadariaFrontEndApp" Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Início"
        ContentTemplate="{DataTemplate local:InicialPage}"
        Route="InicialPage" />

    <ShellContent
        Title="Login Cliente"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

    <ShellContent
        Title="Cadastro"
        ContentTemplate="{DataTemplate local:CadastroPage}"
        Route="CadastroPage" />

</Shell>